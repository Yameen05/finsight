# Copy to .env and fill in. Never commit .env.

# --- OpenAI ---
OPENAI_API_KEY=

# --- Pinecone (serverless) ---
PINECONE_API_KEY=
PINECONE_INDEX_NAME=finsight-filings
# Cloud + region for serverless index creation
PINECONE_CLOUD=aws
PINECONE_REGION=us-east-1

# --- SEC EDGAR ---
# REQUIRED: SEC returns 403 without a descriptive User-Agent containing contact info.
# Format: "<App name> <contact email>"
SEC_USER_AGENT=FinSight Research your.email@example.com

# --- NewsAPI (Phase 3) ---
# Free Developer plan key from https://newsapi.org. Leave blank to skip the
# news agent entirely (status="skipped"). Free tier: 100 requests/day, 30-day window.
NEWS_API_KEY=
NEWS_LOOKBACK_DAYS=30
NEWS_MAX_ARTICLES=30

# --- Models ---
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini

# --- CORS (comma-separated) ---
CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# --- Operational ---
LOG_LEVEL=INFO
# If set, all non-/health routes require header  X-API-Key: <value>
FINSIGHT_API_KEY=
# slowapi syntax: "<count>/<minute|hour>"
RATE_LIMIT_RESEARCH=30/minute
RATE_LIMIT_FILINGS=60/minute
REQUEST_TIMEOUT_SECONDS=120
# Default is local SQLite. For HA, swap to Postgres async URI:
#   postgresql+asyncpg://user:pass@host/db
DATABASE_URL=sqlite+aiosqlite:///./data/finsight.db

# --- Cost tracking (USD per 1M tokens, update if you swap models) ---
COST_INPUT_PER_MTOK_USD=0.15
COST_OUTPUT_PER_MTOK_USD=0.60
COST_EMBED_PER_MTOK_USD=0.02
